[build]
builder = "DOCKERFILE"
dockerfilePath = "services/auth-stub/Dockerfile"
watchPatterns = ["services/auth-stub/**"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
