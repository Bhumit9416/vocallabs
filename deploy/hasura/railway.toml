[build]
builder = "DOCKERFILE"
dockerfilePath = "deploy/hasura/Dockerfile"

[deploy]
healthcheckPath = "/healthz"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
